services:
  - type: web
    name: yt-downloader-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn webhook_bot:app
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: WEBHOOK_URL
        sync: false
    plan: free
