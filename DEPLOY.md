# راهنمای استقرار ربات روی پلتفرم‌های ابری (Webhook)

## ⚠️ هشدار مهم
ربات دانلود ویدیو **به شدت زمان‌بر** است (۳۰ ثانیه تا چند دقیقه).
بنابراین:

- **Vercel** و **Cloudflare Workers** → **نامناسب** (تایم‌اوت ۱۰-۶۰ ثانیه)
- **Render** و **Railway** → مناسب (تایم‌اوت بالاتر)
- VPS ارزان → بهترین گزینه

---

## بهترین گزینه‌های رایگان / ارزان ۲۰۲۶

| پلتفرم       | رایگان؟          | تایم‌اوت تقریبی | مناسب دانلود ویدیو؟ | توصیه |
|--------------|-------------------|------------------|-----------------------|--------|
| **Render**   | بله (با خواب)    | خوب              | بله                   | ★★★★★ |
| **Railway**  | اعتبار رایگان    | عالی             | بله                   | ★★★★  |
| **Fly.io**   | بله (محدود)      | خوب              | بله                   | ★★★★  |
| **Vercel**   | بله               | ۱۰-۶۰ ثانیه      | خیر                   | ❌     |
| **Cloudflare Workers** | بله     | خیلی کم          | خیر                   | ❌     |
| VPS (Hetzner) | ۳-۵ دلار         | نامحدود         | عالی                  | بهترین |

---

## استقرار روی Render.com (پیشنهاد اصلی)

### مرحله ۱: آماده‌سازی
1. کدهای پروژه را در یک مخزن GitHub آپلود کنید (یا از این پوشه استفاده کنید).
2. فایل‌های زیر باید وجود داشته باشد:
   - `webhook_bot.py`
   - `requirements.txt`
   - `Procfile`
   - `render.yaml` (اختیاری)

### مرحله ۲: ایجاد سرویس در Render
1. به [render.com](https://render.com) بروید و ثبت‌نام کنید.
2. روی **New +** → **Web Service** کلیک کنید.
3. مخزن GitHub خود را انتخاب کنید.
4. تنظیمات:
   - **Name**: `yt-downloader-bot`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn webhook_bot:app`
   - **Plan**: Free

### مرحله ۳: متغیرهای محیطی
در بخش **Environment** این متغیرها را اضافه کنید:

| کلید            | مقدار                                      |
|----------------|-------------------------------------------|
| `BOT_TOKEN`    | توکن ربات از @BotFather                    |
| `WEBHOOK_URL`  | بعداً پر می‌شود (بعد از دیپلوی)           |

### مرحله ۴: دیپلوی
روی **Create Web Service** کلیک کنید.

بعد از اتمام دیپلوی، آدرس سایت را کپی کنید (مثلاً: `https://yt-downloader-bot.onrender.com`)

### مرحله ۵: تنظیم وب‌هوک
1. به این آدرس بروید (جایگزین کنید):
   ```
   https://yt-downloader-bot.onrender.com/set_webhook
   ```
2. باید پیام موفقیت ببینید.

یا از طریق مرورگر:
```
https://api.telegram.org/bot<YOUR_TOKEN>/setWebhook?url=https://yt-downloader-bot.onrender.com/<YOUR_TOKEN>
```

### مرحله ۶: تست
لینک یوتیوب را به ربات بفرستید.

---

## استقرار روی Railway (جایگزین خوب)

1. به [railway.app](https://railway.app) بروید.
2. New Project → Deploy from GitHub.
3. متغیر محیطی `BOT_TOKEN` را اضافه کنید.
4. بعد از دیپلوی، آدرس را بگیرید.
5. وب‌هوک را تنظیم کنید (مانند بالا).

---

## نکات مهم برای همه پلتفرم‌ها

### ۱. تنظیم وب‌هوک
بعد از هر دیپلوی این کار را انجام دهید:
```bash
curl "https://api.telegram.org/bot<TOKEN>/setWebhook?url=<YOUR_URL>/<TOKEN>"
```

### ۲. کوکی‌ها (برای ویدیوهای خصوصی)
فایل `cookies.txt` را آپلود کنید:
- در Render: از بخش **Files** یا متغیر محیطی (کمتر توصیه می‌شود).
- راه بهتر: ویدیوهای کانال خود را به صورت **Unlisted** یا **Public** کنید.

### ۳. جلوگیری از خوابیدن (Render)
رندر بعد از ۱۵ دقیقه inactivity می‌خوابد. برای جلوگیری:
- از UptimeRobot یا مشابه برای پینگ هر ۵ دقیقه استفاده کنید.
- یا پلن پولی بگیرید.

### ۴. محدودیت حجم
هنوز همان محدودیت ۴۵ مگابایت برای ارسال به عنوان ویدیو وجود دارد.

---

## اگر می‌خواهید روی Vercel امتحان کنید (توصیه نمی‌شود)

Vercel فقط برای ربات‌های خیلی ساده (مثل echo) مناسب است.

اگر اصرار دارید:
- باید از Edge Functions یا زمان خیلی کوتاه استفاده کنید.
- دانلود ویدیو **قطعاً** fail می‌کند.

---

## توصیه نهایی من

برای ۲۰۰۰ ویدیو:
1. اول از **bulk_downloader.py** روی کامپیوتر یا VPS استفاده کنید.
2. برای ربات تلگرام: از **Render** یا یک VPS ارزان (Hetzner ~۳ دلار) استفاده کنید.

اگر نیاز به کمک برای دیپلوی روی Render یا Railway داری، بگو تا راهنمایی دقیق‌تری بدم.
